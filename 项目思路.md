# Meet 语音碰面推荐 — 项目思路与踩坑记录

> 本文档梳理 2026-05-31 与 AI 协作构建本项目的完整思路、实现路径及所有踩过的坑。  
> 项目路径：`/Users/hz/ai学习/day2`

---

## 一、项目目标

构建一个**前后端分离**的网页应用：用户用麦克风描述自己和朋友的位置，系统自动推荐合适的碰面地点，并以语音播报结果。

### 核心业务链路

```text
用户录音（前端）
  → 上传音频（POST /api/meet/voice）
  → 百炼 ASR（语音转文字）
  → DeepSeek 槽位提取（提取两人 city + address）
  → 高德 MCP（地理编码 → 中点 POI 搜索 → 路线/距离）
  → DeepSeek 回答生成（口语化推荐文案）
  → 百炼 TTS（文字转语音）
  → 前端播放 + 展示推荐列表
```

### 槽位结构

```json
{
  "user":   { "city": "北京", "address": "中关村" },
  "friend": { "city": "北京", "address": "望京" }
}
```

### 推荐结果结构

每个候选地点包含：名称、地址、双方距离/时长、路线摘要（最多 3 个候选）。

---

## 二、技术选型与目录

| 层级 | 技术 | 目录 | 端口 |
|------|------|------|------|
| 前端 | React + Vite | `frontend/` | **5175** |
| 后端 | FastAPI + uvicorn | `backend/` | **8007** |
| 配置 | pydantic-settings + `.env` | `backend/.env` | — |
| 留档 | JSON / 音频文件 | `backend/Storage/` | — |
| 文档 | Markdown | `docs/` | — |

### 开发代理

前端 `vite.config.js` 将 `/api` 代理到 `http://localhost:8007`，开发时无需写死后端地址。

### 后端模块一览

| 模块 | 文件 | 职责 |
|------|------|------|
| 入口 | `main.py` | FastAPI 应用、CORS、健康检查 |
| 配置 | `config.py` | 仅从 `backend/.env` 读取（排除系统环境变量干扰） |
| 路由 | `routers/meet.py` | 串联完整语音链路 |
| ASR | `services/bailian/asr.py` | 百炼语音识别 |
| TTS | `services/bailian/tts.py` | 百炼语音合成 |
| 槽位 | `services/deepseek/slot_extractor.py` | DeepSeek JSON 槽位提取 |
| 回答 | `services/deepseek/answer_generator.py` | DeepSeek 口语化回答 |
| 高德 MCP | `services/amap/mcp_post_client.py` | POST-only MCP 客户端 |
| 地理编码 | `services/amap/geocode.py` | 别名 + 多候选 + 回退链 |
| 推荐 | `services/amap/recommendation.py` | 中点 POI + 路线计算 |
| 解析 | `services/amap/parsers.py` | MCP/REST 返回体归一化 |
| 工具 | `utils/__init__.py` | 中文日志 + Storage 留档 |

### 文档产出

| 文件 | 内容 |
|------|------|
| `docs/百炼_接口文档.md` | ASR + TTS HTTP 调用规范 |
| `docs/DeepSeek_接口文档.md` | 槽位提取 + 回答生成 |
| `docs/高德MCP_接口文档.md` | MCP 接入、工具列表、留档规范 |
| `.cursor/skills/amap-mcp-service/SKILL.md` | 高德 MCP 开发技能（约束与验收清单） |

---

## 三、对话中的开发阶段

以下按与 AI 对话的时间顺序梳理，便于理解「为什么代码长这样」。

### 阶段 0：需求对齐

- 明确：城市 + 具体地址、多个候选 + 路线/距离/时间。
- 前端放 `frontend/`（React），后端 8007，前端 5175。
- 初期约定：**启动服务、安装依赖由用户自行执行**（后期部分操作改由 AI 代理执行）。

### 阶段 1：前端骨架

实现内容：

- Vite 5175 端口 + `/api` → 8007 代理。
- `VoiceRecorder` + `useVoice`（MediaRecorder 录音）。
- `POST /api/meet/voice` 上传（字段名 `audio`）。
- 展示：识别文本、槽位、推荐列表、TTS 播放（`VoicePlayer`）。
- 页面：`HomePage`（主流程）、`RecommendPage`（详情）。

### 阶段 2：后端最小可用

- FastAPI 框架，配置全部放 `backend/.env`。
- 优先实现：接收音频 → 保存到 `Storage/{uuid}.webm`。
- 接口与前端已有约定对齐。

### 阶段 3：百炼 ASR 接入

- 调用百炼 ASR 将音频转文字。
- 留档：`{uuid}_asr.json`、`{uuid}_asr.txt`。
- 产出 `docs/百炼_接口文档.md`。

### 阶段 4：DeepSeek 槽位 + 高德 MCP 推荐

- DeepSeek HTTP 调用，JSON 模式提取两人位置。
- 高德 MCP：`initialize` → `list_tools` → `call_tool`。
- 完整 MCP 调用链留档：`{uuid}_mcp.json`。
- 产出 `docs/DeepSeek_接口文档.md`、`docs/高德MCP_接口文档.md`。

### 阶段 5：DeepSeek 回答 + 百炼 TTS

- 根据 transcript + slots + recommendations 生成口语化回答。
- TTS 合成音频，返回 `audioBase64` + 保存 wav 到 Storage。
- 留档：`{uuid}_answer.json`、`{uuid}_tts.json`。

### 阶段 6：地理编码增强（踩坑集中区）

- 用户实测「北京西 → 东方明珠」报：`无法地理编码 user 的位置: 北京 北京西`。
- 新增 `geocode.py` 回退链，修复简称、POI 缺坐标、低精度结果等问题（详见第五节）。

---

## 四、接口约定

### 健康检查

```http
GET /api/health
```

返回 `pipeline.ready` 及各步骤（asr / deepseek_slot / amap_mcp / deepseek_answer / bailian_tts）就绪状态。

### 主接口

```http
POST /api/meet/voice
Content-Type: multipart/form-data
字段: audio（音频文件）
```

响应字段（摘要）：

```json
{
  "transcript": "...",
  "slots": { "user": {...}, "friend": {...} },
  "recommendations": [...],
  "answer": "...",
  "audioBase64": "...",
  "audioContentType": "audio/wav",
  "audioUrl": "...",
  "asr": { "storage_path": "..." },
  "mcp_artifact": { "storage_path": "..." }
}
```

### 根路径说明

访问 `http://localhost:8007/` 返回 API 入口说明（避免直接访问报 `Not Found` 不知如何使用）。

---

## 五、踩坑清单（完整）

> 按类别归纳。每条包含：**现象 → 原因 → 修复/规避**。

---

### A. 环境与运维

#### A1. 端口被占用 `Address already in use`

| 项 | 说明 |
|----|------|
| **现象** | 启动 `python main.py` 报 `[Errno 48] Address already in use` |
| **原因** | 8007 已有进程（含 AI 后台启动的实例） |
| **处理** | `lsof -i :8007` 查 PID → `kill $(lsof -t -i :8007)` → 再启动；或直接用已在跑的服务访问 `/api/health` |

#### A2. `kill` 报 `not enough arguments`

| 项 | 说明 |
|----|------|
| **现象** | `kill $(lsof -t -i :8007)` 报错 |
| **原因** | 端口本就没有进程，`lsof -t` 无输出 |
| **处理** | 说明端口空闲，直接启动即可，无需 kill |

#### A3. `cd backend` 报 `no such file or directory`

| 项 | 说明 |
|----|------|
| **现象** | 已在 `backend` 目录又执行 `cd backend` |
| **原因** | 路径重复 |
| **处理** | 看提示符是否已在 backend，直接 `source .venv/bin/activate && python main.py` |

#### A4. 配置被系统环境变量覆盖

| 项 | 说明 |
|----|------|
| **现象** | `.env` 改了但不生效，或读到意外值 |
| **原因** | pydantic-settings 默认会读系统环境变量 |
| **修复** | `config.py` 中 `settings_customise_sources` **仅保留** init + dotenv + file_secret，排除 `env_settings` |

#### A5. 代理干扰 HTTP 请求

| 项 | 说明 |
|----|------|
| **现象** | 调用外部 API 异常、超时 |
| **原因** | 本机 HTTP_PROXY 等环境变量影响 httpx |
| **修复** | `utils/proxy.py` 在请求前 `clear_proxy_env()`；httpx 使用 `trust_env=False` |

---

### B. 前端与 API 访问

#### B1. 访问后端根路径报 `{"detail":"Not Found"}`

| 项 | 说明 |
|----|------|
| **现象** | 浏览器打开 `http://localhost:8007/` 看到 Not Found |
| **原因** | 未定义根路由，用户不知道有哪些接口 |
| **修复** | `main.py` 增加 `GET /`，返回 health / voice / docs 入口说明 |

#### B2. 前端跨域

| 项 | 说明 |
|----|------|
| **规避** | `.env` 配置 `CORS_ORIGINS=http://localhost:5175`；开发环境走 Vite 代理可进一步简化 |

---

### C. 百炼 ASR / TTS

#### C1. ASR 音频格式与大小

| 项 | 说明 |
|----|------|
| **注意** | 前端 MediaRecorder 产出 webm；百炼 ASR 对格式、base64 大小有限制 |
| **配置** | `BAILIAN_ASR_MAX_BASE64_MB`、`MAX_UPLOAD_SIZE_MB` 在 `.env` 可调 |
| **留档** | 原始音频 `{uuid}.webm`，识别结果 `{uuid}_asr.json` |

#### C2. TTS 返回方式

| 项 | 说明 |
|----|------|
| **实现** | 同时返回 `audioBase64`（前端直接播放）和 Storage 本地 wav 文件 |
| **留档** | `{uuid}_tts.json` |

---

### D. DeepSeek

#### D1. `Insufficient Balance` 余额不足

| 项 | 说明 |
|----|------|
| **现象** | 槽位提取或回答生成失败，报 `Insufficient Balance` |
| **原因** | DeepSeek 账户余额不足，**不是代码问题** |
| **处理** | 登录 [DeepSeek 开放平台](https://platform.deepseek.com/) 充值；充值后无需改代码，重启或直接重试 |
| **影响** | 槽位失败则整条链路中断，高德 MCP 不会执行 |

#### D2. 槽位输出简称导致后续地理编码失败

| 项 | 说明 |
|----|------|
| **现象** | ASR 识别「北京西」，DeepSeek 输出 `address: "北京西"` |
| **原因** | Prompt 未强制完整地名 |
| **修复** | `slot_extractor.py` Prompt 增加规则：写「北京西站」而非「北京西」 |
| **兜底** | 后端 `geocode.py` 别名表 + 多候选回退（见 E 节） |

---

### E. 高德 MCP（踩坑最多）

#### E1. `Method not allowed (-32000)` — MCP 只支持 POST

| 项 | 说明 |
|----|------|
| **现象** | `{"error":{"code":-32000,"message":"Method not allowed"}}` |
| **原因** | 高德远端 MCP（`mcp.amap.com`）**只支持 POST JSON-RPC**；官方 Python MCP SDK 在 `initialize` 后会发 **GET** 监听 SSE |
| **误区** | 在浏览器直接打开 `https://mcp.amap.com/mcp?key=...` 也会看到此错误，不代表 Key 无效 |
| **修复** | 新建 `mcp_post_client.py`（`AmapMCPPostClient`），纯 POST，不走 SDK 的 GET/SSE |
| **验证** | 通过 `/api/meet/voice` 或脚本调用，不要浏览器直接访问 MCP URL |

#### E2. 跳过 `list_tools()` 硬编码工具名

| 项 | 说明 |
|----|------|
| **风险** | 工具不存在或升级后名称变化导致调用失败 |
| **规范** | 必须先 `initialize` → `list_tools()`，按真实工具列表选择；留档记录 tools 数组 |

#### E3. MCP 返回体 ≠ REST 返回体

| 项 | 说明 |
|----|------|
| **现象** | MCP 调用成功但解析失败，误判为 Key/服务不可用 |
| **原因** | `maps_geo` 有两种形态：MCP 顶层 `results[]` vs REST 的 `status` + `geocodes[]` |
| **修复** | `parsers.py` 的 `normalize_geo()` 兼容两种结构 |
| **规范** | 解析失败时必须记录返回体顶层 key，不能只写「解析失败」 |

#### E4. 简称地理编码失败 — 「北京西」

| 项 | 说明 |
|----|------|
| **现象** | `无法地理编码 user 的位置: 北京 北京西` |
| **原因** | `maps_geo("北京西")` 返回 `ENGINE_RESPONSE_DATA_ERROR`；完整名「北京西站」可成功 |
| **修复** | `geocode.py`：`ADDRESS_ALIASES` 别名表；候选优先试「北京西站」再试原词 |
| **验证** | `116.322033, 39.894912`（北京西站） |

#### E5. 城市前缀误截断

| 项 | 说明 |
|----|------|
| **现象** | 「北京西」被 `strip_city_prefix` 截成「西」，候选变成 `['西', '北京西', ...]` |
| **原因** | 「北京西」以「北京」开头，剩余「西」仅 1 字 |
| **修复** | 剩余字符 `< 2` 时不截断，保留完整「北京西」 |

#### E6. `maps_text_search` 返回 POI 无坐标

| 项 | 说明 |
|----|------|
| **现象** | 「东方明珠」text_search 有结果但 geocode 仍失败 |
| **原因** | MCP 返回的 POI 列表只有 `id/name/address/photo`，**没有 `location` 字段 |
| **修复** | 再调 `maps_search_detail(id=...)` 取 `location` |
| **验证** | `121.499718, 31.239703`（东方明珠广播电视塔） |

#### E7. 低精度地理编码被当成成功 — 「上海东方明珠」

| 项 | 说明 |
|----|------|
| **现象** | `maps_geo("上海东方明珠")` 返回上海市中心 `121.473667, 31.230525`，level=「省」 |
| **原因** | 接口「成功」但精度太粗，不是真实 POI |
| **修复** | `is_low_quality_geo()` 过滤 level 为省/市/国家的结果；原词优先于「城市+地址」拼接候选 |
| **教训** | **不能只看 HTTP/MCP 成功，要看 level 和 district** |

#### E8. QPS 超限 `CUQPS_HAS_EXCEEDED_THE_LIMIT`

| 项 | 说明 |
|----|------|
| **现象** | 连续多候选 maps_geo 时触发限流 |
| **原因** | 一次 geocode 可能连发 4~6 次 MCP 调用（user + friend 各一轮） |
| **规避** | 别名优先减少无效重试；候选顺序优化（先高命中率）；避免短时间重复压测 |
| **待优化** | 候选间加短 delay 或合并策略（未实现） |

#### E9. REST 地理编码回退局限

| 项 | 说明 |
|----|------|
| **配置** | `AMAP_HTTP_GEOCODE_FALLBACK=true`；Key 用 `AMAP_WEB_SERVICE_KEY` 或回退到 `AMAP_MAPS_API_KEY` |
| **实测** | REST 对「北京西」「东方明珠」返回 None；「上海东方明珠」返回市级坐标（不可靠） |
| **结论** | **主路径应依赖 MCP + text_search + search_detail**；REST 只是最后兜底 |
| **注意** | MCP Key 与 Web 服务 Key 能力可能不同，文档应分开说明 |

#### E10. 密钥泄露风险

| 项 | 说明 |
|----|------|
| **现象** | 对话中 `.env` Key 曾被读出 |
| **规范** | 日志/留档用 `mask_url()` 脱敏；`.env` 不入 Git |
| **建议** | 生产前轮换所有已暴露的 Key |

#### E11. MCP 完整留档

| 项 | 说明 |
|----|------|
| **文件** | `Storage/{uuid}_mcp.json` |
| **内容** | initialize、list_tools、每次 call_tool 的参数摘要与 raw_preview |
| **用途** | 排查 geocode/POI/路线问题时的第一手证据 |
| **例** | 请求 `991f85e98...` 只有 `_slots.json` 无 `_mcp.json` → 说明在 MCP 阶段就失败了 |

---

### F. 推荐逻辑（已知局限）

#### F1. 跨城场景中点 POI 不合理

| 项 | 说明 |
|----|------|
| **现象** | 北京西 ↔ 上海东方明珠，推荐出山东境内「happy coffee」等 |
| **原因** | 当前用两地**地理坐标中点**做 `maps_around_search`，跨城时中点落在中间省份野外 |
| **同城** | 中关村 ↔ 望京 正常（如鸟巢咖啡、北土城地铁站） |
| **待优化** | 跨城应改为：分别在两城搜索 + 按交通 hub 推荐，或明确提示「跨城无法推荐中间碰面点」 |

#### F2. POI 搜索关键词

| 项 | 说明 |
|----|------|
| **配置** | `AMAP_MEET_POI_KEYWORDS=商场\|咖啡厅\|地铁站` |
| **配置** | `AMAP_MEET_POI_RADIUS=3000`、`AMAP_MEET_CANDIDATE_LIMIT=3` |

---

## 六、地理编码回退链（最终方案）

`services/amap/geocode.py` 中对每个地址按序尝试：

```text
1. 构建候选词（别名优先）
   例：北京西 → [北京西站, 北京西]
   例：东方明珠 → [东方明珠, 上海东方明珠, 上海东方明珠站]

2. maps_geo（每个候选）
   └─ 失败或低精度 → 下一个

3. maps_text_search（每个候选）
   └─ POI 无 location → maps_search_detail 补坐标

4. REST geocode（每个候选，需 AMAP_HTTP_GEOCODE_FALLBACK=true）

5. 全部失败 → HTTP 502
   detail: 无法地理编码 {label} 的位置: {city} {address}（已尝试: ...）
```

### 别名表（可扩展）

```python
ADDRESS_ALIASES = {
    "北京西": "北京西站",
    "北京南": "北京南站",
    "北京东": "北京东站",
    "北京北": "北京北站",
    "上海南": "上海南站",
    "上海虹桥": "虹桥火车站",
}
```

---

## 七、Storage 留档规范

每次完整请求会在 `backend/Storage/` 生成（`{uuid}` 为音频文件名 stem）：

| 文件 | 内容 |
|------|------|
| `{uuid}.webm` | 原始录音 |
| `{uuid}_asr.json` / `.txt` | ASR 原文与原始响应 |
| `{uuid}_slots.json` | DeepSeek 槽位提取 |
| `{uuid}_mcp.json` | 高德 MCP 完整调用链 |
| `{uuid}_answer.json` | DeepSeek 回答 |
| `{uuid}_tts.json` | TTS 元数据 |
| `{uuid}.wav`（或类似） | 合成语音文件 |

**排查顺序建议：**

1. `_asr.txt` — 识别是否准确  
2. `_slots.json` — 城市/地址是否正确  
3. `_mcp.json` — geocode 哪一步失败、POI 是否合理  
4. `_answer.json` — 文案是否符合推荐结果  

---

## 八、环境变量速查

详见 `backend/.env.example`。核心项：

```env
# 服务
APP_PORT=8007
CORS_ORIGINS=http://localhost:5175
STORAGE_DIR=Storage

# 百炼（ASR + TTS）
DASHSCOPE_API_KEY=

# DeepSeek（槽位 + 回答）
DEEPSEEK_API_KEY=

# 高德 MCP
AMAP_MCP_ENABLED=true
AMAP_MAPS_API_KEY=          # 或 AMAP_MCP_URL 完整 URL
AMAP_WEB_SERVICE_KEY=         # REST 回退（可留空，回退用 MAPS Key）
AMAP_HTTP_GEOCODE_FALLBACK=true
AMAP_GEOCODE_DEFAULT_CITY=北京
AMAP_MEET_POI_KEYWORDS=商场|咖啡厅|地铁站
AMAP_MEET_POI_RADIUS=3000
AMAP_MEET_CANDIDATE_LIMIT=3
AMAP_DIRECTION_TOOL=maps_direction_driving

# 链路开关
PIPELINE_ASR_ENABLED=true
PIPELINE_SLOT_ENABLED=true
PIPELINE_AMAP_ENABLED=true
PIPELINE_ANSWER_ENABLED=true
PIPELINE_TTS_ENABLED=true
```

---

## 九、启动命令

```bash
# 后端
cd backend
source .venv/bin/activate
pip install -r requirements.txt   # 首次
python main.py                  # 8007，热重载由 APP_RELOAD 控制

# 前端
cd frontend
npm install                     # 首次
npm run dev                     # 5175
```

验证：

```bash
curl -s http://localhost:8007/api/health | python3 -m json.tool
```

---

## 十、待办与后续方向

| 优先级 | 项 | 说明 |
|--------|-----|------|
| 高 | 跨城推荐策略 | 避免地理中点落在野外 |
| 中 | QPS 友好 | geocode 候选间节流、减少冗余调用 |
| 中 | 前端错误提示 | 502 geocode 失败时展示「已尝试候选」 |
| 中 | Key 轮换 | 对话中暴露过的密钥应更换 |
| 低 | 槽位 Prompt 持续调优 | 减少简称、歧义地名 |
| 低 | 别名表扩展 | 各地火车站/机场简称 |

---

## 十一、关键结论（给未来自己的话）

1. **高德 MCP 必须用 POST-only 客户端**，不要用官方 SDK 默认的 Streamable HTTP（会发 GET）。
2. **地理编码没有银弹**：简称、POI 缺坐标、低精度「成功」三类问题要分别处理。
3. **MCP 留档是排查生命线**：没有 `_mcp.json` 说明链路在中途断了。
4. **DeepSeek 余额是外部依赖**，报 `Insufficient Balance` 先充值再看代码。
5. **跨城碰面不能简单取中点**，这是产品逻辑问题，不是调参能解决的。
6. **配置只信 `backend/.env`**，并避免系统环境变量污染。

---

## 附录：各节点用户提示词（原文）

> 按开发节点整理，内容为当日对话中你输入的提示词原文（密钥类信息已脱敏）。  
> 系统生成的 `Briefly inform the user...` 类消息已省略。

---

### 节点 0 · 需求对齐

**提示词 1 — 项目背景（先理解、不动手）**

```text
我们接下来要一起构建一个前后端分离网页应用，应用逻辑链路如下：
网页应用打开之后我可以麦克风录入我的声音（描述我和朋友的位置），
前端记录声音文件传给一个后端接口，
后端接口接收这个声音文件先调用百炼平台的ASR模型将声音转文本
之后调用Deepseek对文本内容进行槽位提取（提取两人的位置）
结合Deepseek提取的槽位信息，调用高德MCP服务产出结果（计算相遇地点推荐），
然后根据高德返回结果再走一遍Deepseek模型生成回答，
最后调用阿里云百炼平台的TTS语音模型合成出声音，在前端界面读出来。
这就是这整个项目的背景，请你理解一下，先别动手，理解一下并复述，确认我们对齐了之后，我们再一步步开始。
注意：开发过程中，你不可以执行启动服务，安装依赖等指令，这些指令由我执行，测试也由我执行
```

**提示词 2 — 补充需求细节**

```text
1.还要城市、具体地址
2.多个候选 + 路线/距离/时间
3.语音转文本(ASR)-调用阿里云百炼平台的ASR模型，
将音频转换为文字
4.槽位提取-调用Deepseek模型，从文本中提取结构化信息
(两人的位置
5.React在meet文件夹
```

---

### 节点 1 · 前端开发

**提示词 3**

```text
我们现在先来开发前端，前端代码放在@frontend 文件夹下，现在已经有react框架的前端页面，工作在5175端口，后续开发的所有后端请求都会在8007端口。
```

---

### 节点 2 · 依赖安装与服务启动

**提示词 4**

```text
前端依赖安装，前端服务启动。后端依赖安装，后端服务启动。
```

---

### 节点 3 · 后端骨架（FastAPI + Storage）

**提示词 5**

```text
使用fastapi框架构建后端服务，后端服务工作在8007端口上且所有后端服务的相关配置都放在.env文件中。
优先实现一个功能：前端接收音频之后，传递给后端，后端保存到Storage文件夹下。
```

---

### 节点 4 · 运维与终端排查

**提示词 6 — 端口占用报错**

```text
@/Users/hz/.cursor/projects/Users-hz-ai-day2/terminals/1.txt:76-77 啥意思
```

**提示词 7 — 查端口**

```text
lsof -i :8007
```

**提示词 8 — cd 路径报错**

```text
@/Users/hz/.cursor/projects/Users-hz-ai-day2/terminals/1.txt:86-88
```

**提示词 9 — 收回终端控制权**

```text
关掉你启动的全部终端，指令由我执行。
```

**提示词 10 — 只启后端**

```text
现在我只想启动后端。
```

**提示词 11 — kill 无参数报错**

```text
@/Users/hz/.cursor/projects/Users-hz-ai-day2/terminals/1.txt:138-140
```

---

### 节点 5 · 百炼 ASR 接入

**提示词 12 — ASR 开发规范与接入**

```text
接下来搜索百炼平台的开发规范，并且整理需要用到的接口将开发规范整理进百炼_接口文档.md文件中，存放在docs文件夹下。
根据这个开发规范，接入百炼平台的Qwen-ASR Flash模型，对前端传入的音频做识别。将ASR的模型输出同样保存在Storage文件夹下。
后端的配置都从后端的配置文件.env中读取，不要从环境变量中读取。
调用百炼的服务，不要使用SDK，使用HTTP请求即可（使用BASE64格式上传音频进行转换）。
在初始化客户端之前，清除所有环境变量中的代理变量。
```

**提示词 13 — 写入百炼 Key**

```text
DASHSCOPE_API_KEY=sk-***（原文为完整 Key）

写入 backend/.env
```

**提示词 14 — 重启后端**

```text
重新启动后端
```

**提示词 15 — Not Found 排查**

```text
{"detail":"Not Found"}看看吧
```

---

### 节点 6 · DeepSeek 槽位 + 高德 MCP

**提示词 16 — DeepSeek 文档 + 槽位 + 高德 MCP + 日志**

```text
检索Deepseek的开发规范，同样将文档整理保存在docs文件夹下。ASR识别之后调用Deepseek模型进行提取，提取用户Query中的两个地址，用来做参数传给高德MCP服务接口。

注意这里调用高德一定要构建MCP服务，设置本地的MCP Client连接远端MCP Server。（MCPServer的接收和返回日志要完整储存在Storage中，供我查看）
为后端添加中文简洁日志，让我能够跟踪到这些节点运行状态与对应的输出
```

**提示词 17 — 写入 DeepSeek Key**

```text
deepseek key=sk-***（原文为完整 Key）
```

**提示词 18 — 重启测槽位**

```text
若后端正在运行，请重启后再测试槽位提取：
python main.py
```

**提示词 19 — 整合 ASR → DeepSeek → 高德 MCP**

```text
整合所有服务：ASR→Deepseek→高德
MCP。应该调用高德MCP了。先更新配置文件。
```

**提示词 20 — 写入高德 Key**

```text
高德密钥是：***（原文为完整 Key）
```

**提示词 21 — MCP Method not allowed 报错**

```text
MCP 远端地址报错：{"id":"null","error":{"code":-32000,"message":"Method not allowed"},"jsonrpc":"2.0"}
```

**提示词 22 — 重启后端再测**

```text
重启后端后再测：
cd backend
source .venv/bin/activate
python main.py
```

**提示词 23 — 启动前端**

```text
启动前端
```

---

### 节点 7 · DeepSeek 余额

**提示词 24 — 余额不足**

```text
DeepSeek 调用失败: Insufficient Balance
```

**提示词 25 — 充值完成**

```text
充值完成了deepseek
```

---

### 节点 8 · DeepSeek 回答 + 百炼 TTS（全链路收尾）

**提示词 26**

```text
继续完善后端：将高德MCP返回的结果传递给Deepseek模型整合一个输出，再接上百炼qwen系列TTS模型服务输出语音
```

**提示词 27 — 重启全链路**

```text
请 重启后端 后再测：

cd backend
source .venv/bin/activate
python main.py
```

---

### 节点 9 · 地理编码踩坑

**提示词 28 — 北京西 geocode 失败（出现 2 次）**

```text
无法地理编码 user 的位置: 北京 北京西
```

---

### 节点 10 · 项目复盘

**提示词 29 — 梳理项目与踩坑**

```text
把我今天这个项目涉及到跟你对话的部分，进行梳理出来。踩过的坑都要产出。新建文件：项目思路.md
```

**提示词 30 — 补充各节点提示词（本条）**

```text
把每个节点我输入的提示词产出
```

---

### 提示词与链路节点对照表

| 链路节点 | 对应提示词编号 | 主要产出 |
|----------|----------------|----------|
| 需求对齐 | 1、2 | 项目范围、槽位结构、React 前端 |
| 前端 | 3 | `frontend/` 录音、上传、展示 |
| 环境启动 | 4、6~11、14、18、22、27 | 5175 / 8007 服务 |
| 后端 Storage | 5 | FastAPI + 音频保存 |
| 百炼 ASR | 12、13、15 | `docs/百炼_接口文档.md`、`_asr.json` |
| DeepSeek 槽位 | 16、17、24、25 | `docs/DeepSeek_接口文档.md`、`_slots.json` |
| 高德 MCP | 16、19~21 | `docs/高德MCP_接口文档.md`、`_mcp.json`、`mcp_post_client.py` |
| DeepSeek 回答 + TTS | 26 | `_answer.json`、`_tts.json`、`audioBase64` |
| 地理编码增强 | 28 | `services/amap/geocode.py` |
| 文档复盘 | 29、30 | `项目思路.md` |

---

*文档生成日期：2026-05-31 · 基于当日与 Cursor AI 的完整对话与代码状态整理*
